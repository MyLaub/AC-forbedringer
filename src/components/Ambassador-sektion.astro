---
import Knap from "./Knap.astro";
---

<section class="ambassador-section">
  <div class="ambassador-text">
    <h3>Bliv ambassadør for</h3>
    <h1 class="logo">ANOTHER CLOSET</h1>
    <p>Post 2 billeder på instagram, tag @AnotherCloset.dk og få gratis medlemskab!</p>
    <Knap indhold="Tildmeld dig her!" href="/" color="black" />
  </div>
</section>

<style>
  .ambassador-section {
    display: grid;
    grid-template-columns: 1fr 1fr;
    color: black;
    padding: 4rem 4rem;
    align-items: center;
    gap: 2rem;

    min-height: 70vh;

    background-image: url("/images/ambassador-baggrund.webp");
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
  }

  .ambassador-text {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    font-family: "futura-pt", sans-serif;
  }

  .logo {
    font-family: "CCMetcon Heavy", sans-serif;
    font-size: 3rem;
    color: black;
  }

  .ambassador-knap {
    display: inline-block;
    border: 1.5px solid black;
    padding: 0.75rem 1.5rem;
    font-family: "futura-pt";
    font-size: 1.2rem;
    text-decoration: none;
    color: black;
    border-radius: 999px;
    transition: background-color 0.2s ease;
    width: fit-content;
  }

  .ambassador-knap:hover {
    background-color: black;
    color: white;
  }

  .ambassador-image img {
    width: 100%;
    height: auto;
    object-fit: contain;
    display: block;
    max-height: none;
    aspect-ratio: auto;
  }

  /* Mobil layout */

  @media (max-width: 768px) {
    .ambassador-section {
      display: flex;
      flex-direction: column;
      padding: 2rem 0;

      min-height: 100vh;

      background-image: url("/images/ambassador-baggrund-mobil.webp");
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
    }

    .ambassador-text {
      text-align: left;
      padding-left: 2rem;
      padding-right: 2rem;
    }

    .logo {
      font-size: 2rem;
    }

    .ambassador-image {
      padding: 0 2rem;
    }

    .ambassador-image img {
      width: 100%;
      height: auto;
      object-fit: cover;
      display: block;
    }
  }
</style>
